One-line: The EGU hero mark — the green Gumption “G” core with a slowly orbiting gold constellation (the universe of apps bound by GRIT).

```jsx
<HeroLogo size={180} />            {/* animated orbit (SMIL) */}
<HeroLogo size={96} animate={false} />  {/* still, for print / reduced-motion */}
```

The centerpiece of the hub landing. Pair with the “The Extended Gumption Universe” wordmark (Righteous, gold) beneath. For a flat `<img>`/og-image use, point at `assets/egu-hero.svg` (static). Green core = Gumption/chain identity; gold satellites = the broader EGU.
